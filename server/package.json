// this package.json file allows us to freeze the
// particular versions of each dependency so that
// they harmonize together without breaking.
{
  "name": "workoutlogserver",
  "version": "1.0.0",
  "description": "server for workout log app",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",

    "start": "node ./node_modules/http-server/bin/http-server"
  },
  "author": "Julie Simpson",
  "license": "MIT",
  "dependencies": {
  	"body-parser": "^1.15.1",
  	"express": "^4.13.4",
  	"pg": "^4.5.6",
  	"pg-hstore": "^2.3.2",
  	"sequelize": "^3.23.3"
  }
}
